<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
      <mapper namespace="com.crystal.SpringBootPractise.dao.ProductDao">
 <!--   <resultMap id="balanceMap" type="com.crystal.SpringBootPractise.domain.Balance">
        <result column="id" property="id"></result>
        <result column="username" property="username"></result>
        <result column="user_balance" property="userBalance"/>
    </resultMap>-->
<select id="getProduct" resultType="com.crystal.SpringBootPractise.domain.Product">
    select * from product where id = #{id}
</select>

    <update id="updateProduct" parameterType="com.crystal.SpringBootPractise.domain.Product">
        update product set count = #{count} where id = #{id}
    </update>
</mapper>